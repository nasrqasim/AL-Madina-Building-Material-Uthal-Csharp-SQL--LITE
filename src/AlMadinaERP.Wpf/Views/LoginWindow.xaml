<Window x:Class="AlMadinaERP.Wpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="AL MADINA BUILDING MATERIAL - System Authentication" 
        Height="620" Width="960"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        x:Name="LoginWin">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Border Background="#0F172A" CornerRadius="16" BorderBrush="#334155" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- TOP TITLE BAR & WINDOW CONTROLS -->
            <Grid Grid.Row="0" Background="#7A0C0C" MouseDown="TitleBar_MouseDown">
                <TextBlock Text="AL MADINA BUILDING MATERIAL - ERP AUTHENTICATION" Foreground="#FFFFFF" FontSize="11" FontWeight="Bold" VerticalAlignment="Center" Margin="16,0,0,0"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0">
                    <Button Click="Minimize_Click" Content="—" Background="Transparent" Foreground="#FFFFFF" FontSize="12" FontWeight="Bold" Width="28" Height="28" Padding="0" Margin="2,0" Cursor="Hand" BorderThickness="0"/>
                    <Button Click="Close_Click" Content="✕" Background="#DC2626" Foreground="#FFFFFF" FontSize="12" FontWeight="Bold" Width="28" Height="28" Padding="0" Margin="2,0" Cursor="Hand" BorderThickness="0"/>
                </StackPanel>
            </Grid>

            <!-- MAIN CONTENT AREA -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="460"/>
                </Grid.ColumnDefinitions>

                <!-- LEFT BRANDING SIDE -->
                <Border Grid.Column="0" Background="#1E293B" Padding="40">
                    <Grid VerticalAlignment="Center">
                        <StackPanel>
                            <Border Background="#7A0C0C" CornerRadius="16" Width="72" Height="72" HorizontalAlignment="Left" Margin="0,0,0,24">
                                <TextBlock Text="AM" Foreground="#FFFFFF" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <TextBlock Text="AL MADINA" Foreground="#FFFFFF" FontSize="32" FontWeight="Bold"/>
                            <TextBlock Text="BUILDING MATERIAL" Foreground="#FDE047" FontSize="22" FontWeight="Bold" Margin="0,2,0,12"/>
                            
                            <TextBlock Text="Wholesale &amp; Retail ERP System" Foreground="#94A3B8" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,24"/>

                            <Separator Background="#334155" Margin="0,0,0,24"/>

                            <StackPanel Margin="0,0,0,8">
                                <TextBlock Text="✓ Secure Offline Desktop Architecture" Foreground="#CBD5E1" FontSize="12" Margin="0,4"/>
                                <TextBlock Text="✓ Full Accounting &amp; Ledger Synchronization" Foreground="#CBD5E1" FontSize="12" Margin="0,4"/>
                                <TextBlock Text="✓ Real-time Inventory &amp; POS Counter Control" Foreground="#CBD5E1" FontSize="12" Margin="0,4"/>
                                <TextBlock Text="✓ High Performance Local SQLite Database" Foreground="#CBD5E1" FontSize="12" Margin="0,4"/>
                            </StackPanel>

                            <TextBlock Text="Software By: Roonjha Developers - 03152914836" Foreground="#64748B" FontSize="10" FontWeight="SemiBold" Margin="0,40,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- RIGHT LOGIN FORM CARD -->
                <Border Grid.Column="1" Background="#FFFFFF" Padding="44">
                    <Grid VerticalAlignment="Center">
                        <StackPanel>
                            <TextBlock Text="Sign In to Your Account" Foreground="#0F172A" FontSize="22" FontWeight="Bold" Margin="0,0,0,6"/>
                            <TextBlock Text="Enter your authorized credentials to access the ERP." Foreground="#64748B" FontSize="12" Margin="0,0,0,28"/>

                            <!-- USERNAME INPUT -->
                            <TextBlock Text="Username *" Foreground="#475569" FontSize="11" FontWeight="Bold" Margin="0,0,0,6"/>
                            <Border BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="6" Padding="4" Margin="0,0,0,18" Background="#F8FAFC">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="32"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="👤" Foreground="#64748B" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    <TextBox Grid.Column="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" FontSize="13" Padding="6,8" BorderThickness="0" Background="Transparent" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>

                            <!-- PASSWORD INPUT WITH SHOW/HIDE TOGGLE -->
                            <DockPanel Margin="0,0,0,6">
                                <TextBlock Text="Password *" Foreground="#475569" FontSize="11" FontWeight="Bold"/>
                                <Button Command="{Binding TogglePasswordVisibilityCommand}" Content="{Binding IsPasswordVisible, Converter={StaticResource BooleanToVisibilityConverter}}" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Cursor="Hand">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Content" Value="👁️ Show Password"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsPasswordVisible}" Value="True">
                                                    <Setter Property="Content" Value="🔒 Hide Password"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </DockPanel>

                            <Border BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="6" Padding="4" Margin="0,0,0,24" Background="#F8FAFC">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="32"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="🔑" Foreground="#64748B" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    
                                    <!-- PasswordBox when Hidden -->
                                    <PasswordBox x:Name="TxtPasswordBox" Grid.Column="1" PasswordChanged="PasswordBox_PasswordChanged" FontSize="13" Padding="6,8" BorderThickness="0" Background="Transparent" VerticalAlignment="Center">
                                        <PasswordBox.Style>
                                            <Style TargetType="PasswordBox">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsPasswordVisible}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </PasswordBox.Style>
                                    </PasswordBox>

                                    <!-- TextBox when Visible -->
                                    <TextBox Grid.Column="1" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" FontSize="13" Padding="6,8" BorderThickness="0" Background="Transparent" VerticalAlignment="Center">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsPasswordVisible}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                </Grid>
                            </Border>

                            <!-- ERROR MESSAGE -->
                            <TextBlock Text="{Binding ErrorMessage}" Foreground="#DC2626" FontSize="12" FontWeight="Bold" Margin="0,0,0,16" TextWrapping="Wrap" Visibility="{Binding ErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                            <!-- LOGIN BUTTON -->
                            <Button Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=LoginWin}" IsDefault="True" Content="SIGN IN TO ERP" Background="#7A0C0C" Foreground="#FFFFFF" FontSize="13" FontWeight="Bold" Padding="16,12" Cursor="Hand">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="6"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
